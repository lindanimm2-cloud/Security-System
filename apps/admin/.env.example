# Admin / public site (Next.js)

NEXT_PUBLIC_API_URL=http://localhost:4010/v1

# Vercel demo: set this to your deployed API base (include /v1), e.g.
# NEXT_PUBLIC_API_URL=https://your-api.example.com/v1
#
# Without a reachable API, store catalog, login, and portals will fail in the browser.
