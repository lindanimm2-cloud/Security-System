# Deprecated — use `/public/vehicles/`

Vehicle GLB assets now live per make/model:

```text
/public/vehicles/<make-model-slug>/model.glb
```

See [`../vehicles/README.md`](../vehicles/README.md).

A generic `/models/vehicle.glb` is no longer used. The viewer never falls back to procedural geometry.
